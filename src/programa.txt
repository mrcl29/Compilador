/// Crea i executa aquí el teu programa

tuple x = {logical, logical};

fnct integer suma(a::integer, b::integer):
rtrn (a+b);
endfnct

fnct logical main():
out(suma(1,2));
rtrn(TRUE);
endfnct

main()
